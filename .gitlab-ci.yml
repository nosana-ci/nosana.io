image: node:14

stages:
  - test
  - deploy

test:
  stage: test
  script:
    - npm ci
    - npm run lint
  only:
    - merge_requests
    - main

pages:
  stage: deploy
  script:
    - npm ci
    - npm run generate
    - mv dist public
  artifacts:
    paths:
      - public
  only:
    - main
